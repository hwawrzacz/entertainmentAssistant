<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="actionOverflowButtonStyle">@style/MyOverflowButtonStyle</item>
        <item name="buttonStyle">@style/buttonDisableCaps</item>
    </style>

    <!-- Splash screen -->
    <style name="AppTheme.Launcher" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@drawable/launch_screen</item>
    </style>

    <!-- Account bottom sheet dialog theme -->
    <style name="AccountBottomSheetDialog" parent="@style/Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowIsFloating">false</item>
        <item name="bottomSheetStyle">@style/AccountBottomSheet</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
    <style name="AccountBottomSheet" parent="@style/Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:layout_marginLeft">@dimen/margin_regular</item>
        <item name="android:layout_marginRight">@dimen/margin_regular</item>
        <item name="android:layout_marginBottom">0dp</item>
    </style>
    <style name="AccountBottomSheetDialogTheme" parent="AccountBottomSheetDialog" />

    <!-- Full screen dialog fragment -->
    <style name="AppTheme.FullScreenDialogFragment" parent="Theme.AppCompat.DayNight.DialogWhenLarge">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:toolbarStyle">@style/ThemeOverlay.MaterialComponents.Toolbar.Primary</item>
    </style>

    <!-- Light icon on action bar -->
    <style name="MyOverflowButtonStyle" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/text_on_primary</item>
    </style>

    <!--  Disable default button text capitalization  -->
    <style name="buttonDisableCaps" parent="Widget.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
    </style>
</resources>
